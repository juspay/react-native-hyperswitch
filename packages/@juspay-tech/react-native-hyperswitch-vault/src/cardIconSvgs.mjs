/*
 * The two glyphs the asset host does not serve (cvv.svg and camera.svg return 403), inlined as
 * markup so no consumer needs a metro SVG transformer and nothing is copied into the tarball.
 *
 * Paste the SVG source between the backticks. An empty string renders nothing.
 */

export const cvv = ``;

export const camera = ``;
